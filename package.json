{
  "name": "template-config",
  "version": "0.0.1",
  "description": "An envoriment based configuration with template strings for process.env variables.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "configuration",
    "envoriments",
    "templating"
  ],
  "author": "Emil Cardell",
  "license": "MIT",
  "devDependencies": {
    "expect.js": "^0.3.1"
  }
}
